<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Sign in</h1>
  <%= render 'layouts/validation_errors',  validation_errors: @errors if @errors.present? %>

  <%= form_with url: session_url, class: "contents" do |form| %>
    <%= render 'session_fields', form: form %>

    <div class="col-span-6 sm:flex sm:items-center sm:gap-4">
      <div class="inline">
        <%= form.submit "Sign in", class: "w-full sm:w-auto text-center rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
      </div>

      <div class="mt-4 text-sm text-gray-500 sm:mt-0">
        <%= link_to "Forgot password?", new_password_path, class: "text-gray-700 underline hover:no-underline" %>
      </div>
    </div>

    <div class="col-span-6 sm:flex sm:items-center sm:gap-4 pt-3">
      <div class="mt-4 text-sm text-gray-500 sm:mt-0">
        Don't have an account?, <%= link_to "Sign Up", signup_path, class: "text-gray-700 underline hover:no-underline" %>
      </div>
    </div>
  <% end %>
</div>
